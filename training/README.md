# Training

This folder contains a minimal public template for continuing LoRA training. It does not include the private source images, exact resume checkpoints, optimizer state, scheduler state, or RNG state.

Use `dataset_example.jsonl` as the format reference. Put real training images outside the public repository and point the dataset records at those local files.
